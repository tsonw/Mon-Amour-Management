rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
    	
      // Cho phép đọc, chỉnh sửa và xoá khi đã đăng nhập - Thay đổi ngày 30/7
      allow read, write, update, delete: if request.auth != null;
      
    }
  }
}
